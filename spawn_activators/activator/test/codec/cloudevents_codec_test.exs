defmodule Activator.Codec.CloudEventTest do
  use ExUnit.Case

  # alias Activator.Codec.CloudEvent, as: Codec
  # alias Io.Cloudevents.V1.CloudEvent

  # test "decode should return cloudevent module" do
  #   cloudevent_struct = CloudEvent.new(id: "test", type: "test-type")
  #   encoded = CloudEvent.encode(cloudevent_struct)
  #   IO.inspect(is_binary(encoded), label: "Is Binary ------ ")
  #   IO.inspect(encoded, label: "Encoded ------ ")

  #   decoded = Codec.decode(encoded)
  #   IO.inspect(decoded, label: "Decoded ------ ")
  #   assert true
  # end
end
